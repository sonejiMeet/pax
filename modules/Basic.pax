// Basic.pax 
// this module is implicitly imported by Pax frontend by looking inside this baked in modules folder
// add stuff here that should be available to every program without an explicit import
exit :: (status: int) #foreign;
__rdtsc :: () -> u64 #foreign;

string :: struct {
    count: u64;
    data: ^u8;
}

